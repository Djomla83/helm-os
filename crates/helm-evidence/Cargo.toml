[package]
name = "helm-evidence"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
publish = false
description = "Experimental read-only verifier for declared evidence-bundle contracts"

[dependencies]
cap-std = "=4.0.2"
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.149"
sha2 = "=0.10.9"

[lints]
workspace = true
